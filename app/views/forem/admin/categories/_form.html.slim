= simple_form_for [forem, :admin, @category], :html => {:class => 'form-horizontal'} do |f|
  fieldset
    = f.input :name
  .form-actions  
    = f.button :submit, :class => 'btn btn-primary'
    |  
    = link_to "Відміна", forem.admin_categories_path, :class => "btn"

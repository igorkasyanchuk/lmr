=link_to 'New Page', new_resource_path
table
  thead
    th title
    th path
    th edit
  tbody
    - collection.each do |page|
      tr
        td= link_to page.title, resource_path(page)
        td= page_path(page.identifier)
        td= link_to 'edit', edit_resource_path(page)
